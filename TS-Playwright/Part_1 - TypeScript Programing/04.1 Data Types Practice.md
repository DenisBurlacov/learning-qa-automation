---
dataviewjs: false
---

# 🏋️ Практика — TypeScript: Типы данных

> **Как работать:** Читай задание → Пробуй сам → Застрял? → Читай подсказку → Проверяй по решению

---

## 📦 ЧАСТЬ 1: number

### Задание 1.1 — Базовые числа и операции

**Цель:** Попрактиковаться в создании числовых переменных и базовых операциях.

**Шаги:**
1. Создай файл `NumberDemo.ts`
2. Объяви переменные с аннотацией типа `number`:
   - `age` — твой возраст
   - `height` — твой рост в см (дробное)
   - `temperature` — температура за окном (может быть отрицательной)
3. Выведи все три переменные с подписями через запятую в `console.log`
4. Создай переменную `nextYearAge` — твой возраст через год (через вычисление, не вручную)
5. Выведи все четыре переменные через шаблонную строку в одну строку

<details>
<summary>💡 Подсказка</summary>

Через запятую в `console.log`: `console.log("Возраст:", age)`
Через шаблонную строку: `` console.log(`Возраст: ${age}`) ``
Следующий год: `let nextYearAge: number = age + 1`
</details>

<details>
<summary>✅ Проверь себя</summary>

```typescript
let age: number = 40;
let height: number = 180.5;
let temperature: number = -3;

console.log("Возраст:", age);        // Возраст: 40
console.log("Рост:", height);        // Рост: 180.5
console.log("Температура:", temperature); // Температура: -3

let nextYearAge: number = age + 1;

console.log(`Мне ${age} лет, рост ${height} см, за окном ${temperature}°C, через год будет ${nextYearAge}`);
```
</details>

---

### Задание 1.2 — Калькулятор

**Цель:** Применить математические операции с числами.

**Сценарий:** Ты покупаешь 3 товара в интернет-магазине.

**Шаги:**
1. Создай переменные `price1`, `price2`, `price3` с ценами (придумай сам)
2. Вычисли `subtotal` — сумма всех трёх товаров
3. Вычисли `discount` — скидка 15% от subtotal
4. Вычисли `shipping` — доставка: если subtotal > 100, то 0, иначе 5.99 (используй тернарный оператор)
5. Вычисли `total` — итоговая сумма с учётом скидки и доставки
6. Выведи красивый чек через шаблонные строки

<details>
<summary>💡 Подсказка</summary>

Скидка 15%: `let discount: number = subtotal * 0.15`
Тернарный оператор: `let shipping: number = subtotal > 100 ? 0 : 5.99`
</details>

<details>
<summary>✅ Проверь себя</summary>

```typescript
let price1: number = 49.99;
let price2: number = 29.99;
let price3: number = 14.99;

let subtotal: number = price1 + price2 + price3;
let discount: number = subtotal * 0.15;
let shipping: number = subtotal > 100 ? 0 : 5.99;
let total: number = subtotal - discount + shipping;

console.log("===== ЧЕК =====");
console.log(`Товар 1: ${price1}€`);
console.log(`Товар 2: ${price2}€`);
console.log(`Товар 3: ${price3}€`);
console.log(`Подытог: ${subtotal}€`);
console.log(`Скидка 15%: -${discount}€`);
console.log(`Доставка: ${shipping}€`);
console.log(`ИТОГО: ${total}€`);
```
</details>

---

### Задание 1.3 — typeof для чисел

**Цель:** Научиться проверять тип переменной через `typeof`.

**Шаги:**
1. Создай три числовые переменные: целое число, дробное, отрицательное
2. Для каждой выведи значение и её тип через `typeof`
3. Попробуй присвоить строку в числовую переменную — что скажет TypeScript?

<details>
<summary>✅ Проверь себя</summary>

```typescript
let integer: number = 42;
let floating: number = 3.14;
let negative: number = -100;

console.log(`Значение: ${integer}, Тип: ${typeof integer}`);   // number
console.log(`Значение: ${floating}, Тип: ${typeof floating}`); // number
console.log(`Значение: ${negative}, Тип: ${typeof negative}`); // number

// Попытка присвоить строку:
// integer = "текст"; // ❌ TypeScript Error: Type 'string' is not assignable to type 'number'
```
</details>

---

## 📝 ЧАСТЬ 2: string

### Задание 2.1 — Три способа создать строку

**Цель:** Попрактиковать все три способа объявления строк.

**Шаги:**
1. Создай переменную `firstName` через двойные кавычки
2. Создай переменную `lastName` через одинарные кавычки
3. Создай переменную `fullName` через backtick, вставив `firstName` и `lastName` внутрь
4. Создай переменную `greeting` через backtick с приветствием и числовой переменной `age`
5. Выведи все четыре переменные

<details>
<summary>✅ Проверь себя</summary>

```typescript
let firstName: string = "John";
let lastName: string = 'Kennedy';

let fullName: string = `${firstName} ${lastName}`;
console.log(fullName); // John Kennedy

let age: number = 35;
let greeting: string = `Привет! Меня зовут ${firstName} ${lastName} и мне ${age} лет.`;
console.log(greeting); // Привет! Меня зовут John Kennedy и мне 35 лет.
```
</details>

---

### Задание 2.2 — Выражения внутри backtick

**Цель:** Научиться писать выражения и вызовы методов внутри `${}`.

**Шаги:**
1. Создай переменные `a = 15` и `b = 4` (number)
2. Выведи одной строкой через backtick: сумму, разность, произведение, остаток от деления
3. Создай переменную `username: string = "alice"` (строчными)
4. Выведи строку где имя написано заглавными буквами — прямо внутри `${}`
5. Создай `score: number = 72` и выведи строку: "Результат: 72 — Сдал" или "Результат: 72 — Не сдал" (порог 60), используя тернарный оператор внутри `${}`

<details>
<summary>💡 Подсказка</summary>

Метод заглавных букв: `username.toUpperCase()`
Тернарный оператор внутри: `` `${score >= 60 ? "Сдал" : "Не сдал"}` ``
</details>

<details>
<summary>✅ Проверь себя</summary>

```typescript
let a: number = 15;
let b: number = 4;

console.log(`${a} + ${b} = ${a + b}`);   // 15 + 4 = 19
console.log(`${a} - ${b} = ${a - b}`);   // 15 - 4 = 11
console.log(`${a} * ${b} = ${a * b}`);   // 15 * 4 = 60
console.log(`${a} % ${b} = ${a % b}`);   // 15 % 4 = 3

let username: string = "alice";
console.log(`Имя пользователя: ${username.toUpperCase()}`); // ALICE

let score: number = 72;
console.log(`Результат: ${score} — ${score >= 60 ? "Сдал" : "Не сдал"}`);
// Результат: 72 — Сдал
```
</details>

---

### Задание 2.3 — Динамический URL для теста

**Цель:** Применить строки в реальном сценарии автоматизации.

**Сценарий:** Ты пишешь автотест, который проверяет страницу профиля пользователя.

**Шаги:**
1. Создай переменные: `baseUrl`, `userId` (number), `section` (например "settings")
2. Собери `profileUrl` из этих переменных: `https://app.com/users/42/settings`
3. Создай переменные для лога теста: `browser`, `testName`, `passed` (boolean)
4. Собери строку лога: `[Chrome] ProfileTest: PASSED` или `FAILED` в зависимости от `passed`
5. Выведи оба результата

<details>
<summary>✅ Проверь себя</summary>

```typescript
let baseUrl: string = "https://app.com";
let userId: number = 42;
let section: string = "settings";

let profileUrl: string = `${baseUrl}/users/${userId}/${section}`;
console.log(profileUrl); // https://app.com/users/42/settings

let browser: string = "Chrome";
let testName: string = "ProfileTest";
let passed: boolean = true;

let logEntry: string = `[${browser}] ${testName}: ${passed ? "PASSED" : "FAILED"}`;
console.log(logEntry); // [Chrome] ProfileTest: PASSED
```
</details>

---

## ✅ ЧАСТЬ 3: boolean

### Задание 3.1 — Булевы флаги

**Цель:** Создать boolean переменные и использовать их в условиях.

**Шаги:**
1. Создай переменные: `isLoggedIn = true`, `isAdmin = false`, `hasPremium = true`, `isEmailVerified = true`
2. Через `if/else` выведи разные сообщения в зависимости от `isLoggedIn`
3. Создай переменную `canAccessDashboard: boolean` — пользователь может войти в панель только если залогинен И верифицирован email
4. Создай `canManageUsers: boolean` — только если залогинен, верифицирован И является админом
5. Выведи оба результата с подписями

<details>
<summary>💡 Подсказка</summary>

Логическое И: `&&` — оба должны быть `true`
Логическое ИЛИ: `||` — хотя бы одно `true`
</details>

<details>
<summary>✅ Проверь себя</summary>

```typescript
let isLoggedIn: boolean = true;
let isAdmin: boolean = false;
let hasPremium: boolean = true;
let isEmailVerified: boolean = true;

if (isLoggedIn) {
    console.log("Добро пожаловать в систему!");
} else {
    console.log("Пожалуйста, войдите в систему");
}

let canAccessDashboard: boolean = isLoggedIn && isEmailVerified;
let canManageUsers: boolean = isLoggedIn && isEmailVerified && isAdmin;

console.log(`Доступ к дашборду: ${canAccessDashboard}`); // true
console.log(`Управление пользователями: ${canManageUsers}`); // false
```
</details>

---

### Задание 3.2 — Boolean из сравнений

**Цель:** Получать boolean значения как результат сравнений.

**Сценарий:** Система проверки кандидата на вакансию.

**Шаги:**
1. Создай: `yearsExperience: number = 3`, `age: number = 25`, `testScore: number = 78`, `speaksEnglish: boolean = true`
2. Вычисли через сравнения (не пиши вручную true/false):
   - `hasSufficientExp` — опыт больше 2 лет
   - `isRightAge` — возраст от 22 до 45
   - `passedTest` — результат теста выше 70
3. Вычисли `isHired: boolean` — принят если все три условия выполнены И говорит по-английски
4. Выведи итог: "Принят" или "Отклонён"

<details>
<summary>✅ Проверь себя</summary>

```typescript
let yearsExperience: number = 3;
let age: number = 25;
let testScore: number = 78;
let speaksEnglish: boolean = true;

let hasSufficientExp: boolean = yearsExperience > 2;   // true
let isRightAge: boolean = age >= 22 && age <= 45;       // true
let passedTest: boolean = testScore > 70;               // true

let isHired: boolean = hasSufficientExp && isRightAge && passedTest && speaksEnglish;

console.log(`Опыт достаточный: ${hasSufficientExp}`);
console.log(`Подходящий возраст: ${isRightAge}`);
console.log(`Тест сдан: ${passedTest}`);
console.log(`Решение: ${isHired ? "✅ Принят" : "❌ Отклонён"}`);
```
</details>

---

## ❓ ЧАСТЬ 4: null и undefined

### Задание 4.1 — Когда значения нет

**Цель:** Понять разницу между null и undefined на практике.

**Шаги:**
1. Объяви переменную `notInitialized: number` без значения — выведи её (что получишь?)
2. Создай `emptyValue: null = null` — выведи значение и тип через `typeof`
3. Создай `noValue: undefined = undefined` — выведи значение и тип через `typeof`
4. Сравни `null == undefined` и `null === undefined` — что получишь?

<details>
<summary>✅ Проверь себя</summary>

```typescript
let notInitialized: number;
// console.log(notInitialized); // TS предупредит, но выведет: undefined

let emptyValue: null = null;
console.log(emptyValue);         // null
console.log(typeof emptyValue);  // "object" (историческая особенность JS)

let noValue: undefined = undefined;
console.log(noValue);            // undefined
console.log(typeof noValue);     // "undefined"

// Сравнение:
console.log(null == undefined);  // true  — нестрогое
console.log(null === undefined); // false — строгое (разные типы!)
```
</details>

---

### Задание 4.2 — null в union type (практический сценарий)

**Цель:** Применить `string | null` в реальной ситуации.

**Сценарий:** Система авторизации — пользователь может быть залогинен или нет.

**Шаги:**
1. Объяви `currentUser: string | null = null` — пользователь не залогинен
2. Через `if/else` выведи "Гость" если `null`, иначе "Привет, [имя]!"
3. Симулируй вход — присвой имя пользователя
4. Снова выведи через `if/else`
5. Симулируй выход — верни `null`
6. Снова выведи

<details>
<summary>✅ Проверь себя</summary>

```typescript
let currentUser: string | null = null;

// Шаг 1: не залогинен
if (currentUser === null) {
    console.log("Привет, Гость!");
} else {
    console.log(`Привет, ${currentUser}!`);
}
// Привет, Гость!

// Шаг 2: вход
currentUser = "Alice";
if (currentUser === null) {
    console.log("Привет, Гость!");
} else {
    console.log(`Привет, ${currentUser}!`);
}
// Привет, Alice!

// Шаг 3: выход
currentUser = null;
console.log(currentUser === null ? "Привет, Гость!" : `Привет, ${currentUser}!`);
// Привет, Гость!
```
</details>

---

## 🌀 ЧАСТЬ 5: any

### Задание 5.1 — Опасность any

**Цель:** Увидеть своими глазами почему any — опасен.

**Шаги:**
1. Создай `value: any = "100"` (строка)
2. Попробуй сделать `value * 2` и выведи результат — что получишь?
3. Теперь присвой `value = 100` (число) и снова `value * 2` — что теперь?
4. Присвой `value = true` — выведи тип через `typeof`
5. Сделай вывод: почему TypeScript и сам any не защищает от ошибки в шаге 2?

<details>
<summary>✅ Проверь себя</summary>

```typescript
let value: any = "100"; // строка!

// Шаг 2: строка * 2:
console.log(value * 2); // NaN — строку нельзя умножать!
// Но TypeScript не предупредил нас!

// Шаг 3: теперь число:
value = 100;
console.log(value * 2); // 200 — теперь правильно

// Шаг 4: boolean:
value = true;
console.log(typeof value); // "boolean"

// Вывод: any отключает Type Safety.
// TypeScript видит any и думает "ладно, проверять не буду".
// Из-за этого ошибка в шаге 2 не была поймана на этапе написания кода.
// Именно поэтому any — плохой выбор.
```
</details>

---

### Задание 5.2 — any vs конкретный тип

**Цель:** Сравнить защиту при `any` и при конкретном типе.

**Шаги:**
1. Создай `price1: any = "99.99"` — попробуй умножить на 1.21 (НДС), выведи
2. Создай `price2: number = 99.99` — умножь на 1.21, выведи
3. Попробуй присвоить строку в `price2` — что скажет TypeScript?
4. Запиши вывод: когда TypeScript защитил, а когда нет?

<details>
<summary>✅ Проверь себя</summary>

```typescript
let price1: any = "99.99";
console.log(price1 * 1.21); // NaN — строка × число = NaN, но ошибки нет

let price2: number = 99.99;
console.log(price2 * 1.21); // 120.888... — правильно!

// price2 = "дорого"; // ❌ ОШИБКА! TypeScript защищает:
// Type 'string' is not assignable to type 'number'

// Вывод:
// any → нет защиты, ошибки скрыты, результат непредсказуем
// number → полная защита, TypeScript не позволит положить строку
```
</details>

---

## 🔗 ЧАСТЬ 6: Union Type

### Задание 6.1 — Union вместо any

**Цель:** Заменить any на union там где это возможно.

**Шаги:**
1. Тебе нужна переменная `productId` — она может быть числом (42) или строкой ("SKU-42")
2. Сначала объяви её через `any` — присвой число, потом строку
3. Теперь перепиши через union — только `number | string`
4. Попробуй присвоить boolean в union-версию — что скажет TypeScript?
5. Выведи оба варианта и сравни защиту

<details>
<summary>✅ Проверь себя</summary>

```typescript
// Через any (плохо):
let productIdAny: any = 42;
productIdAny = "SKU-42"; // ok
productIdAny = true;     // ok — но мы этого не хотели!

// Через union (хорошо):
let productId: number | string = 42;
productId = "SKU-42"; // ✅ разрешено

// productId = true; // ❌ ОШИБКА — boolean не разрешён!
// Type 'boolean' is not assignable to type 'string | number'

console.log(productId); // SKU-42
```
</details>

---

### Задание 6.2 — Система статусов

**Цель:** Применить union type для моделирования реальной системы.

**Сценарий:** Статус заказа в интернет-магазине может быть числовым кодом или строкой.

**Шаги:**
1. Создай `orderStatus: number | string | null = null` — заказ не создан
2. Присвой статус как число `1` (создан)
3. Выведи: если null — "Нет заказа", иначе — "Статус заказа: [значение]"
4. Поменяй статус на строку `"processing"` — выведи снова
5. Попробуй присвоить boolean — что будет?

<details>
<summary>✅ Проверь себя</summary>

```typescript
let orderStatus: number | string | null = null;

// Нет заказа:
console.log(orderStatus === null ? "Нет заказа" : `Статус заказа: ${orderStatus}`);
// Нет заказа

// Создан:
orderStatus = 1;
console.log(orderStatus === null ? "Нет заказа" : `Статус заказа: ${orderStatus}`);
// Статус заказа: 1

// В обработке:
orderStatus = "processing";
console.log(orderStatus === null ? "Нет заказа" : `Статус заказа: ${orderStatus}`);
// Статус заказа: processing

// orderStatus = true; // ❌ ОШИБКА — boolean не в union
```
</details>

---

## ⚙️ ЧАСТЬ 7: void и функции

### Задание 7.1 — Функции с void

**Цель:** Написать функции которые ничего не возвращают.

**Шаги:**
1. Напиши функцию `printDivider(): void` — выводит строку из 20 символов `=`
2. Напиши функцию `printHeader(title: string): void` — выводит заголовок между разделителями
3. Напиши функцию `printUserCard(name: string, age: number, isActive: boolean): void` — выводит карточку пользователя
4. Вызови все три функции

<details>
<summary>✅ Проверь себя</summary>

```typescript
function printDivider(): void {
    console.log("====================");
}

function printHeader(title: string): void {
    printDivider();
    console.log(title);
    printDivider();
}

function printUserCard(name: string, age: number, isActive: boolean): void {
    printDivider();
    console.log(`Имя: ${name}`);
    console.log(`Возраст: ${age}`);
    console.log(`Статус: ${isActive ? "Активен" : "Неактивен"}`);
    printDivider();
}

printHeader("Список пользователей");
printUserCard("Alice", 30, true);
printUserCard("Bob", 25, false);
```
</details>

---

### Задание 7.2 — Функции с возвращаемым значением

**Цель:** Написать функции с разными возвращаемыми типами.

**Шаги:**
1. Напиши `calculateTotal(price: number, quantity: number): number` — возвращает сумму
2. Напиши `formatPrice(price: number): string` — возвращает строку вида "99.99€"
3. Напиши `isDiscounted(price: number): boolean` — возвращает true если цена > 50
4. Напиши `findProduct(id: number): string | null` — возвращает название товара по id (1→"Laptop", 2→"Phone", остальные→null)
5. Вызови все функции и красиво выведи результаты

<details>
<summary>✅ Проверь себя</summary>

```typescript
function calculateTotal(price: number, quantity: number): number {
    return price * quantity;
}

function formatPrice(price: number): string {
    return `${price}€`;
}

function isDiscounted(price: number): boolean {
    return price > 50;
}

function findProduct(id: number): string | null {
    if (id === 1) return "Laptop";
    if (id === 2) return "Phone";
    return null;
}

let total: number = calculateTotal(29.99, 3);
let formatted: string = formatPrice(total);
let discounted: boolean = isDiscounted(total);
let product: string | null = findProduct(1);

console.log(`Итого: ${formatted}`);           // Итого: 89.97€
console.log(`Есть скидка: ${discounted}`);    // Есть скидка: true
console.log(`Товар: ${product ?? "не найден"}`); // Товар: Laptop
console.log(`Товар #5: ${findProduct(5) ?? "не найден"}`); // не найден
```
</details>

---

## 🚀 ЧАСТЬ 8: Комплексные задачи

### Задание 8.1 — Выбери правильный тип

**Цель:** Научиться выбирать подходящий тип для каждой переменной.

**Для каждой переменной ниже — выбери тип и объясни почему:**

1. Максимальное количество попыток входа: `___` = 5
2. Текущий пользователь (может отсутствовать): `___` = null
3. Текст кнопки: `___` = "Submit"
4. Пользователь аутентифицирован: `___` = false
5. ID транзакции (может быть числом или строкой): `___` = "TXN-001"
6. Версия приложения: `___` = "2.1.0"
7. Счётчик кликов: `___` = 0
8. Результат поиска (строка или null если не нашли): `___` = null

<details>
<summary>✅ Проверь себя</summary>

```typescript
const maxLoginAttempts: number = 5;          // number — фиксированное число
let currentUser: string | null = null;        // string | null — может отсутствовать
const buttonText: string = "Submit";          // string — текст
let isAuthenticated: boolean = false;         // boolean — флаг
let transactionId: number | string = "TXN-001"; // union — оба типа возможны
const appVersion: string = "2.1.0";          // string — версия это текст, не число
let clickCount: number = 0;                  // number — счётчик
let searchResult: string | null = null;      // string | null — или результат или пусто
```
</details>

---

### Задание 8.2 — Профиль пользователя с типами

**Цель:** Применить все типы в связном примере.

**Шаги:**
1. Создай переменные для профиля пользователя используя ПРАВИЛЬНЫЕ типы:
   - Имя (string)
   - Возраст (number)
   - Email (string)
   - Количество публикаций (number)
   - Аккаунт активен (boolean)
   - Премиум подписка (boolean)
   - Дата последнего входа (string | null — null если никогда)
2. Напиши функцию `printProfile(...)` которая принимает все эти данные и красиво их выводит
3. Вызови функцию с реальными данными

<details>
<summary>✅ Проверь себя</summary>

```typescript
function printProfile(
    name: string,
    age: number,
    email: string,
    postsCount: number,
    isActive: boolean,
    isPremium: boolean,
    lastLogin: string | null
): void {
    console.log("========== ПРОФИЛЬ ==========");
    console.log(`Имя: ${name}`);
    console.log(`Возраст: ${age}`);
    console.log(`Email: ${email}`);
    console.log(`Публикаций: ${postsCount}`);
    console.log(`Статус: ${isActive ? "Активен" : "Неактивен"}`);
    console.log(`Подписка: ${isPremium ? "Премиум" : "Обычная"}`);
    console.log(`Последний вход: ${lastLogin ?? "никогда"}`);
    console.log("=============================");
}

printProfile("Alice", 28, "alice@example.com", 42, true, true, "2024-03-15");
printProfile("Bob", 35, "bob@example.com", 0, false, false, null);
```
</details>

---

### Задание 8.3 — Найди и исправь ошибки

**Цель:** Научиться распознавать проблемы с типами.

**В коде ниже есть ошибки типизации. Найди их все и исправь:**

```typescript
let userName: string = 42;

let isActive: boolean = "true";

const MAX_RETRIES: number = "5";

let score: number = 85;
score = "отлично";

let userId: number | string = 123;
userId = true;

function greet(name: string): void {
    return `Hello, ${name}!`;
}

function getAge(): number {
    return "тридцать";
}
```

<details>
<summary>✅ Проверь себя</summary>

```typescript
// Ошибка 1: число в string переменной
let userName: string = "Alice"; // ✅ строка в строку

// Ошибка 2: строка "true" в boolean
let isActive: boolean = true; // ✅ настоящий boolean

// Ошибка 3: строка "5" в number
const MAX_RETRIES: number = 5; // ✅ число в число

// Ошибка 4: строка в number переменную
let score: number = 85;
// score = "отлично"; // нельзя — если нужен статус, используй другую переменную
let scoreLabel: string = "отлично"; // ✅ отдельная строковая переменная

// Ошибка 5: boolean не входит в number | string
let userId: number | string = 123;
// userId = true; // нельзя — boolean не в union
userId = "user-123"; // ✅ строка разрешена

// Ошибка 6: void функция возвращает значение
function greet(name: string): string { // ✅ тип возврата string, не void
    return `Hello, ${name}!`;
}

// Ошибка 7: функция с типом number возвращает строку
function getAge(): number {
    return 30; // ✅ число, не строку
}
```
</details>

---

## 📊 Чеклист самопроверки

После выполнения всех заданий отметь что понял:

- [ ] Я понимаю разницу между динамической (JS) и статической (TS) типизацией
- [ ] Я знаю что такое type safety и могу привести пример
- [ ] Я понимаю разницу между **annotation** (явно) и **inference** (автоматически)
- [ ] Я умею создавать `number` переменные и делать с ними операции
- [ ] Я знаю три способа создать `string` и когда использовать backtick
- [ ] Я умею вставлять переменные и выражения внутрь строки через `${}`
- [ ] Я знаю что `boolean` хранит только `true`/`false`
- [ ] Я понимаю разницу между `null` (намеренно пусто) и `undefined` (не присвоено)
- [ ] Я знаю почему `any` опасен и когда его можно использовать
- [ ] Я умею создавать union types через `|` и понимаю чем они лучше `any`
- [ ] Я умею писать функции с `void` (без return) и с конкретным типом возврата
- [ ] Я могу выбрать правильный тип для переменной в реальной ситуации

---

> 💪 **Совет:** Не подглядывай в ответы раньше времени. Ошибиться и разобраться — это и есть обучение. TypeScript покажет тебе ошибку прямо в редакторе — читай сообщение и думай что оно означает.
